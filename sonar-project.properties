sonar.projectKey=TransitOps-NovaFleet
sonar.projectName=TransitOps-NovaFleet
sonar.organization=darshan1814

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=src

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

# Exclude test files or specific directories from analysis
sonar.exclusions=**/*.test.tsx, **/*.test.ts, node_modules/**, .next/**, public/**
